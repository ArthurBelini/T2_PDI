usage: script.py [-h] [-k KERNEL_SIZE]

Filtragem High-Boost

options:
  -h, --help            show this help message and exit
  -k KERNEL_SIZE, --kernel_size KERNEL_SIZE
                        Tamanho da matriz de convolucao (default:
                        5)

Exemplos:
  -